package se.liu.kevri781;

public enum Direction
{
    /**
     * Represents the different directions a character can face.
     * Used to determine which sprite sheet to load for a character.
     * Also used for player movement.
     */
    LEFT, RIGHT, UP, DOWN
}
